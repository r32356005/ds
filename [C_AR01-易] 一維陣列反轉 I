#include <stdio.h>
int main(){
	int  b[100], a, i = 0;
	while (scanf("%d",&a)){
		b[i] = a;
		i++;
	}
	while (i > 0) {
		i--;
		printf("%d",b[i]);
		if (i != 0) cout << " ";
		if (i == 0) cout << endl;
	}
	return 0;
}
