#include <stdio.h>

int main() {
    int x, y;
    while (scanf("%d %d", &x, &y) == 2) {
        if ((x > 100 || x < 0) || (y > 100 || y < 0)) {
            printf("outside\n");
        } else {
            printf("inside\n");
        }
    }
    return 0;
}
