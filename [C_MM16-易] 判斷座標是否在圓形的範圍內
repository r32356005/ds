#include <stdio.h>
#include <math.h>

int main() {
    int x, y, dis;
    while (scanf("%d %d", &x, &y) == 2) {
        dis = pow(x, 2) + pow(y, 2);
        if (dis > 40000) {
            printf("outside\n");
        } else {
            printf("inside\n");
        }
    }
    return 0;
}
